{
  "name": "resonant-agent",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "RESONANT Agent harness, onboarding, and local surfaces built on the Pi runtime.",
  "scripts": {
    "ui": "node ui/server.js",
    "telegram:setup": "node telegram/setup.js",
    "telegram:start": "node telegram/bridge.js",
    "check": "node --check bridge/pi-rpc.js && node --check ui/server.js && node --check telegram/setup.js && node --check telegram/bridge.js && node --check scripts/write-config.js && node --check scripts/auth-env.js"
  },
  "engines": {
    "node": ">=18"
  }
}
